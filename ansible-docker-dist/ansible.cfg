[defaults]
inventory        = inventory/hosts.ini
roles_path       = roles
remote_user      = bhayangkara
host_key_checking = False
stdout_callback  = default
#stdout_callback  = yaml
retry_files_enabled = False

# Penting: SCP untuk transfer file
[ssh_connection]
transfer_method = scp
